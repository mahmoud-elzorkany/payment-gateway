/**
 * Note this file should be a .env file in a real application.
 * Where the constants are environment variables configured by the deployment environment.
 */

export const PORT_NUMBER = 3000;
export const DATA_DIR = "data";
